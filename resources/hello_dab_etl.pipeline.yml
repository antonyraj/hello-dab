# The main pipeline for hello_dab

resources:
  pipelines:
    hello_dab_etl:
      name: hello_dab_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      root_path: "../src/hello_dab_etl"

      libraries:
        - glob:
            include: ../src/hello_dab_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
